export const ContainerPadding = 0.05;
export const LabelSize = UDim2.fromScale(1, 0.1);
export const LabelSizeHalfWidth = UDim2.fromScale(1 / 2, LabelSize.Height.Scale);
export const LabelSizeThirdWidth = UDim2.fromScale(1 / 3, LabelSize.Height.Scale);

export const ContainedLabelSize = UDim2.fromScale(1, 0.1);
export const ContainedLabelSizeHalfWidth = UDim2.fromScale(0.5, 1);
export const ContainedLabelSizeHeightFull = UDim2.fromScale(1, 1);
