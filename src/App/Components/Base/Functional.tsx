import Roact from "@rbxts/roact";

// Template

interface TemplateProps {
    name: string;
}

const Template: Roact.FunctionComponent<TemplateProps> = (props) => {
    return (
        <frame
        />
    );
}

export default Template